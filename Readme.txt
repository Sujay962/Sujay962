$git --version

$git help config / $git config --help

$mkdir git

$cd git

$git init

$git status

$git add Readme.txt

$git commit -m "Creating First File in Repository"

Link a git to Github account
$git config --global user.username Sujay962

Local and remote repositories linked 
$git remote add origin "https://github.com/Sujay962/Sujay962.git"

Push file to github remote repository
$git push origin master

List the configurations
$git config --list

configurations
$git config --global user.name "Sujay962"
$git config --global user.email 'sujay962@gmail.com'

Make changes in file 
$git diff




$git branch first_branch
$git checkout first_branch

$git add info3.txt
$git commit -m "Made changes in first_branch"
$ls
$git checkout master
$ls
$git merge first_branch
ls
